@using Microsoft.AspNetCore.Http;
@inject IHttpContextAccessor HttpContextAccessor;
@model List<wwfpp.Models.Admin.MenusViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    var PageTitle = "Menus";
    var Page_Tips = "";  //Tool tip message on right side
    var msg_please_wait = "";
    var Page_Mode = PageTitle; 
    ViewData["Title"] = "| " + PageTitle;
    var Help_Tips = GblUtilities.HelpTips(Page_Tips);    
    string ShowMsg = GblUtilities.MsgDisplay(Context.Request.Query["msg"].ToString());/*success|fail|error'*/
}
@Html.AntiForgeryToken()
@Html.Hidden("ilaka", "/Admin/Menus")
<h2 class="titlebig"><span name="header" id="header">@Page_Mode</span> @Html.Raw(Help_Tips)</h2>
@Html.Hidden("mode", "")
@Html.Hidden("Id", "")

<div id="message_header"><p id="loading" class="loading displaynone">@msg_please_wait</p></div>
<div id="message_header">@Html.Raw(ShowMsg)</div>

<div class="row">
    <div class="col-lg-2"><label class="col-lg-12 col-form-label-lg">Module : &nbsp;</label></div>
    <div class="col-lg-10">
        @Html.DropDownList("ModuleFilter", (SelectList)ViewBag.ModuleFilter, "-- Select --", new { @class = "form-select", style = "width:300px;" })
    </div>
</div>

<!-- Controls row under the header -->
<div class="p-0 bg-white rounded mb-3">
    <div class="row align-items-center">
        <div class="col-auto ms-auto">
            @Html.Raw(ViewBag.ViewButtons)
        </div>
    </div>
</div>
<!-- DataTable -->
@if (Model == null)
{
    <div id="message_header"><p class="note">No record(s) found.</p></div>
}
else
{
    <div class="form-group p-2">
        <table id="tblData" class="display compact nowrap" style="width:100%">
            <thead>
                <tr style="height: 40px; align-items: center;color:white;" class="bg-secondary bg-opacity-27">
                    <th width="1%"><input type="checkbox" id="selectAll"></th>
                    <th width="5%">Code</th>
                    <th width="20%">Name</th>
                    <th width="20%">Label</th>
                    <th width="14%">Page</th>
                    <th width="10%">Sorting</th>
                    <th width="5%">Status</th>
                    <th width="15%">Module</th>
                    <th width="5%">Action</th>
                </tr>
            </thead>
            <tbody>
                <!-- Data dynamically injected here -->
            </tbody>
        </table>
    </div>

    <!-- Modal for Add/Edit -->
    @await Html.PartialAsync("_SharedModal", new wwfpp.Models.ModalViewModel { ModalId = "ModalID", DialogClass = "modal-lg", BodyId = "ModalBody", })

    @section scripts 
    {
        <script>
            var ilaka = document.getElementById('ilaka').value;
            var selectedIds = new Set();
            var table;
            $(document).ready(function () 
            {
                GetAllData(ilaka, "menu_id", [
                    { data: "menu_code", name: "menu_code", autoWidth: false },
                    { data: "menu_name", name: "menu_name", autoWidth: false },
                    { data: "menu_label", name: "menu_label", autoWidth: false },
                    { data: "menu_page", name: "menu_page", autoWidth: false },
                    { data: "menu_sort", name: "menu_sort", autoWidth: false },
                    { data: "menu_status", name: "menu_status", autoWidth: false },
                    { data: "module_label", name: "module_label", autoWidth: false }                    
                ],
                {
                    FilterValue : "ModuleFilter"
                });
                $(document).on("click", "#tblData .button", function () {ViewData($(this).data("id"));});
                $(document).off('click', '#btnC').on('click', '#btnC', function () {modalCloseCancel($(this));});
            });
             $("#ModuleFilter").off("change").on("change", function () {table.draw();});
        </script>
    }
}
